-> It is used to perform auth into the applicaion.

->It is an library in Spring framework which provides Authorization grant type (for web apps) and implecit grant type (for single-page apps).

->OAuth2 is an authorization framework that enables the application Web Security to access the resources from the client.

->To implement this we need to make clear concept about  Grant Type (Authorization code), Client ID and Client secret.

->Client Id and Clint_Secret will generated by the google cloud console for Google logging itself.

->It provides to the developer to implement (social login) GOOGLE, FACEBOOK login.

->Mainly GOOGLE API gives to the developer a TOKEN by which application can match user and logging into the page.

->We cannot define our own endpoints here. But we can do so with the help of OpenId connect having some restriction.